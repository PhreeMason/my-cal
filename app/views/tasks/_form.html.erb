<% if task.errors.any? %>
  <div id="error_explanation">
    <h3>
      <%= pluralize(task.errors.count, "error") %>
      prohibited this product from being saved:
    </h3>

   <ul>
    <% task.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<%= f.label :content %></br>
<div class="field <%= 'field_with_errors' if task.errors[:content].any? %>">
  <%= f.text_field :content %></br>
</div>

<%= f.hidden_field :calendar_id, value: my_cal.id %>
<%= f.label 'Time' %></br>

<div class="field <%= 'field_with_errors' if task.errors[:start_time].any? %>" id='time' >
  <%= datetime_local_field(:date, :hour) %>
</div>
<%= content_tag :div, class: 'actions' do %>
<%= f.submit %></br>
<% end %>
