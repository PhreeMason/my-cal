<nav class="">
  <ul>
      <% if logged_in? %>
    <li> <%= "Signed in as #{current_user.username}" %> | <%= link_to "Sign out", destroy_user_session_path, method: :delete %> | <%= link_to 'Last Month', "/months/#{@month.id - 1}" %> | <%= link_to 'Next Month', "/months/#{@month.id + 1}" %></li>
      <%else%>
    <li> <%= link_to "Sign in", new_user_session_path %> |  <%= link_to "Sign up", new_user_registration_path %> </li>
      <% end %>
  </ul>
</nav>
