<nav class="">
  <ul>
      <% if logged_in? %>
    <li> <%= "Signed in as #{current_user.username}" %> | <%= link_to "Sign out", destroy_user_session_path, method: :delete %> | <%= link_to 'Last Month', prev_month(@month) %> | <%= link_to 'Next Month', next_month(@month) %></li>
      <%else%>
    <li> <%= link_to "Sign in", new_user_session_path %> |  <%= link_to "Sign up", new_user_registration_path %> </li>
      <% end %>
  </ul>
</nav>
